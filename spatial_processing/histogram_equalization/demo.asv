clc; close all; close all;

%% load image
path = 'D:\github\ImageProcessing\spatial_processing\data\';
file_name = 'low_contrast_img.png';
image = imread([path, file_name]);

%% histogram equalization

data = image(:);

pixel_distribution = zeros(256, 1);

for i = 1:size(data)
    pxiel_distribtion(data(i) + 1) = pxiel_distribtion(data(i) + 1) + 1;
end